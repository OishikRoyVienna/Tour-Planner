# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/mrp
spring.datasource.username=mrp
spring.datasource.password=mrp
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 🔧 WICHTIG: Schema für PostgreSQL festlegen
//spring.jpa.properties.hibernate.default_schema=public

# File Upload Path
app.upload.path=./uploads

# OpenRouteService API Key (https://openrouteservice.org)
ors.api.key=DEIN_ORS_API_KEY_HIER